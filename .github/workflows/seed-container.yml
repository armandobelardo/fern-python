name: seed_container

on:
  workflow_call:

jobs:
  seed_docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install Dependencies
        run: docker build . -f docker/seed/Dockerfile -t fernapi/python-seed
